# ── Frontend (Vite) Environment Variables ──────────────────────
# Copy this file to .env and fill in the values

# API URL for local development with LAN access (optional)
# Leave empty on Vercel (uses relative /api path)
VITE_API_URL=

{
  "name": "behold-aspire-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "test": "npx concurrently \"npm run test --prefix backend\" \"npm run test --prefix frontend\"",
    "lint": "npx concurrently \"npm run lint --prefix backend\" \"npm run lint --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "mongodb": "^7.4.0"
  }
}
